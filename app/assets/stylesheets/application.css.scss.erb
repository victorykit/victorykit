/* FIXME
 *= require twitter/bootstrap
 *= require twitter/bootstrap-responsive
 *= require dataTables/jquery.dataTables.bootstrap
 *= require bootstrap-wysihtml5
 *= require chosen
 *= require font-awesome
 *= require_tree .
 */

// @import "bootstrap";
// @import "bootstrap-responsive";
// @import "jquery.dataTables.bootstrap";
// @import "bootstrap-wysihtml5";
// @import "chosen";
// @import "font-awesome";
// @import "**/*";

* {
  box-sizing: border-box;
  -moz-box-sizing: border-box;
}

input {
  height: 30px !important;
}

.no-trailing {
  margin-bottom: 0;
}

.container.main {
  margin-top: 20px
}

#footer {
  font-size: 12px;
  text-align: center;
  background: #efefef;
  border-top: 1px solid #eee;
}

.progress_box {
  padding: 10px;
  margin-bottom: 10px;
  &.red-bordered, &.red-unbordered {
    background: #fdf6f6;
    &.red-bordered {
      border: 1px solid #fadada;
    }
  }
}

@mixin progressbar($fluid, $fluid_highlight) {
  height: 20px;
  margin: 5px 0px 10px;
  border-radius: 11px;
  -moz-box-shadow: inset 0 0 5px #ddd;
  -webkit-box-shadow: inset 0 0 5px #ddd;
  box-shadow: inset 0 0 5px #ddd;
  background: #fefefe;
  border: 1px solid #ddd;
  .fluid {
    background: #{$fluid}; /* Old browsers */
    background: -moz-linear-gradient(1deg,  $fluid 0%, $fluid_highlight 100%); /* FF3.6+ */
    background: -webkit-gradient(linear, left bottom, right top, color-stop(0%,$fluid), color-stop(100%,$fluid_highlight)); /* Chrome,Safari4+ */
    background: -webkit-linear-gradient(1deg,  $fluid 0%,$fluid_highlight 100%); /* Chrome10+,Safari5.1+ */
    background: -o-linear-gradient(1deg,  $fluid 0%,$fluid_highlight 100%); /* Opera 11.10+ */
    background: -ms-linear-gradient(1deg,  $fluid 0%,$fluid_highlight 100%); /* IE10+ */
    background: linear-gradient(1deg,  $fluid 0%,$fluid_highlight 100%); /* W3C */
    filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#{$fluid_highlight}', endColorstr='#{$fluid}',GradientType=0 ); /* IE6-8 fallback on horizontal gradient */
    width: 33%;
    height: 98%;
    border: 1px solid #{$fluid};
    -moz-box-shadow: inset 3px -4px 4px 1px $fluid;
    -webkit-box-shadow: inset 3px -4px 4px 1px $fluid;
    box-shadow: inset 3px -4px 4px 1px $fluid;
    border-radius: 11px;
  }
}
.progressbar {
  @include progressbar(#8B1A1A, #CC1100);
}
.progressbar.red {
  @include progressbar(#8B1A1A, #CC1100);
}
.progressbar.blue {
  @include progressbar(#0058E5, #0099ff);
}

.numbers .large_and_bold .signatures {
  color: #8B1A1A;
}

@mixin button($color0, $color1, $color2, $color3) {
  color: white;
  text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.25);
  background-color: $color0;
  background-image: -moz-linear-gradient(top, $color1, $color2);
  background-image: -webkit-gradient(linear, 0 0, 0 100%, from($color1), to($color2));
  background-image: -webkit-linear-gradient(top, $color1, $color2);
  background-image: -o-linear-gradient(top, $color1, $color2);
  background-image: linear-gradient(to bottom, $color1, $color2);
  background-repeat: repeat-x;
  filter: progid:DXImageTransform.Microsoft.gradient(startColorstr='#ff0088cc', endColorstr='#ff0044cc', GradientType=0);
  border-color: $color2 $color2 $color3;
  border-color: rgba(0, 0, 0, 0.1) rgba(0, 0, 0, 0.1) fadein(rgba(0, 0, 0, 0.1), 15%);
  filter: progid:DXImageTransform.Microsoft.gradient(enabled = false);
  &:hover, &:active, &.active, &:disabled, &.disabled {
    background-color: $color3;
    background-image: linear-gradient(to bottom, $color0, $color3);
    background-repeat: repeat-x;
  }
}

.btn-primary {
  @include button(#b31717, #cc1100, #8B1A1A, #721919);
}
.btn-primary.red {
  @include button(#b31717, #cc1100, #8B1A1A, #721919);
}
.btn-primary.blue {
  @include button(#0058E5, #4190E5, #0099ff, #0058E5);
}


input {
  border-radius: 0 !important;
}
.navbar-inner {
  border-radius: 0;
}

abbr[title="required"] {
  cursor: auto;
  border-bottom: none;
}

#header {
  .logo {
    font-family: "LeagueGothicRegular";
    font-size: 3em;
    color: white;
    float: left;
    line-height: 40px;
    &:hover {
      color: #eee;
      text-decoration: none;
    }
  }

  .navbar .nav > li > a {
    cursor: pointer;
  }
}


html, body {
  height: 100%;
}

#wrap {
  min-height: 100%;
  height: auto !important;
  height: 100%;
  margin: 0 auto -60px;
}

#push,
#footer {
  height: 60px;
}

#footer {
  background-color: #f5f5f5;
}

@media (max-width: 767px) {
  #footer {
    margin-left: -20px;
    margin-right: -20px;
    padding-left: 20px;
    padding-right: 20px;
  }
}

span.nav-divider {
  line-height: 34px;
  margin: 0 10px;
  display: inline-block;
  vertical-align: top;
}

.nav-pills.pill-block {
  display: inline-block;
  > li {
    > a                  { background-color: #eee; border-radius: 0; margin-right: 0; }
    &.active > a         { background-color: #0088cc; color: white; }
    > a:hover            { background-color: #0088cc; color: white; }
    &.disabled > a:hover { background-color: #eee; color: #999; }
    &:first-child > a    { border-top-left-radius: 4px; border-bottom-left-radius: 4px; }
    &:last-child > a     { border-top-right-radius: 4px; border-bottom-right-radius: 4px; }
  }
}

i.icon-line:after { content: "―"; }

.legend {
  border: 1px solid #eee;
  border-radius: 4px;
  display: inline-block;
  padding: 6px 16px;
  > span { margin-left: 16px; &:first-child { margin-left: 0; } }
}

iframe.chart {
  border: none;
  height: 180px;
  &[class*="span"] { margin-left: 0; }
}
