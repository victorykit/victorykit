<% flash.each do |type, message| %>

    <div class="alert-message fade in" "data-alert"="alert"  class="<%= twitterized_type(type) %>">
    <a href="#" class="close">x</a>
    <p>
      <%= message %>
    </p>


    </div>

<% end %>