!!!
%html{:lang => "en", "xmlns:fb" => "http://ogp.me/ns/fb#"}
  %head
    %meta{:charset => "utf-8"}
    %meta{"http-equiv" => "X-UA-Compatible", :content => "IE=edge,chrome=1"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    - if browser.ie?
      %link{:href => "/assets/application_ie.css", :media => "screen", :rel => "stylesheet", :type => "text/css"}
    - else
      = stylesheet_link_tag :application
    = javascript_include_tag :application
    %title= content_for?(:title) ? yield(:title) : @title
    = google_analytics_tracker
    = csrf_meta_tags
    = airbrake_javascript_notifier
    = yield :meta_tags

    / Le HTML5 shim, for IE6-8 support of HTML elements
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
  - tweakingbackground = spin! "changing background colors", :share, ["white_background", "bootstrap_ui"]

  %body{class: [tweakingbackground]}
    = render :partial =>  "shared/navigation"
    .container
      .content
        = render :partial => 'shared/flash_messages'
        = yield
    = render :partial =>  "shared/footer"
