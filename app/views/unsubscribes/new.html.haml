%h1 Unsubscribe

%p
  Unsubscribe from future emails:

= simple_form_for(@unsubscribe, :html => {:class => 'form-horizontal' }) do |f|
  = f.input :email, :as => :string, :input_html => { :value => @email }
  = f.button :submit, :class => 'btn btn-primary', :value => 'Unsubscribe'
  - if @email_hash
    = hidden_field_tag :email_hash, @email_hash
%p
  &nbsp;

%p
  - unsubscribe_url, org_name = AppSettings.require_keys!('organization.unsubscribe_url', 'organization.name')
  %a{href: unsubscribe_url}
    To unsubscribe from all emails from #{org_name}, click here.
